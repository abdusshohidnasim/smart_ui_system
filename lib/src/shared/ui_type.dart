/// The color/theme type for any UI component.
enum SmartUIType {
  /// Green — success/completed
  success,

  /// Red — error/failed
  error,

  /// Blue — info/neutral
  info,

  /// Orange — warning/caution
  warning,

  /// Plain dark — Android-style no color
  plain,
}